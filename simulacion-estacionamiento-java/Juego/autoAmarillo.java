import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
public class autoAmarillo extends Actor
{
    public autoAmarillo()
    {
        GreenfootImage img = getImage();
        img.scale(50, 116);
        setImage(img);
    }
}
